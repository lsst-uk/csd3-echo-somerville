name: test env CI

on:
  push:
    branches: [ "128-clean-up-zips-with-dask-k8s" ]
  pull_request:
    branches: [ "128-clean-up-zips-with-dask-k8s" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file echo-side/containers/mamba_env/Dockerfile --tag ghcr.io/lsst-uk/csd3-echo-somerville:$(git rev-parse --short HEAD)-tests --tag ghcr.io/lsst-uk/csd3-echo-somerville:tests
    - name: Push the Docker image
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u lsst-uk --password-stdin && docker push --all-tags ghcr.io/lsst-uk/csd3-echo-somerville